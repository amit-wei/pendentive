#!/bin/sh
# Refuses the commit if the system model is inconsistent.
# Enable once with:  git config core.hooksPath .githooks
python3 model/validate_model.py || {
    echo
    echo "Commit refused: the system model failed validation."
    exit 1
}
