# model

Structured data, checked by `validate_model.py` on every commit. This is the source
of truth for the design.

`model_conventions.md` gives the rules. `product/` holds the decomposition tree.
`registers/` holds what spans every level.
