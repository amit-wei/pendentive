# product

The root of the decomposition tree. Each directory below is one compartment, and
holds its own functions, requirements and risks.

The level of an item is readable from its identifier. `registers/compartments.csv`
declares the code that each directory holds.
